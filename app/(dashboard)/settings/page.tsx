import { ShieldCheck, UserPlus } from "lucide-react";
import { DeleteMeal } from "@/components/delete-meal";
import { formatDate, getWeddingContext } from "@/lib/wedding";
import { addMealOption, updateWedding } from "./actions";
export default async function SettingsPage({searchParams}:{searchParams:Promise<{error?:string;message?:string}>}){const p=await searchParams;const {supabase,wedding}=await getWeddingContext();if(!wedding)return null;const [{data:meals},{data:members,error}]=await Promise.all([supabase.from("meal_options").select("id,name,description,is_active").eq("wedding_id",wedding.id).order("sort_order"),supabase.from("wedding_users").select("id,role,user_id").eq("wedding_id",wedding.id)]);return <div className="page settings-page"><div className="page-header"><div><p className="eyebrow">Workspace</p><h1>Settings</h1><p className="muted dashboard-intro">Wedding details, RSVP options and administrator access.</p></div></div>{(p.error||error)&&<div className="alert alert-error">{p.error||error?.message}</div>}{p.message&&<div className="alert">{p.message}</div>}<div className="settings-layout"><div className="settings-main"><section className="card detail-section"><div className="detail-section-title"><h2>Wedding details</h2></div><form action={updateWedding} className="detail-section-body form-grid"><Field name="couple_names" label="Couple names" value={wedding.couple_names} required/><Field name="wedding_date" label="Wedding date" value={wedding.wedding_date} type="date" required/><Field name="ceremony_time" label="Ceremony time" value={wedding.ceremony_time} type="time"/><Field name="venue" label="Ceremony venue" value={wedding.venue}/><Field name="reception_venue" label="Reception venue" value={wedding.reception_venue}/><div className="field"><label htmlFor="currency">Default currency</label><select className="select" id="currency" name="currency" defaultValue={wedding.currency}><option value="GBP">GBP — British pound</option><option value="EUR">EUR — Euro</option><option value="USD">USD — US dollar</option></select></div><Field name="guest_capacity" label="Guest capacity" value={wedding.guest_capacity} type="number"/><Field name="budget_target" label="Budget target" value={wedding.budget_target} type="number"/><Field name="rsvp_deadline" label="RSVP deadline" value={wedding.rsvp_deadline} type="date"/><Field name="website_url" label="Wedding website URL" value={wedding.website_url} type="url"/><div className="settings-save span-2"><button className="button button-primary">Save settings</button></div></form></section><section className="card detail-section"><div className="detail-section-title"><h2>Meal options</h2></div><div className="detail-section-body"><form action={addMealOption} className="meal-add"><input className="input" name="name" placeholder="e.g. Roast chicken" aria-label="Meal option name" required/><input className="input" name="description" placeholder="Short description" aria-label="Meal description"/><button className="button button-secondary">Add option</button></form>{meals?.length?<div className="meal-list">{meals.map(m=><div key={m.id}><div><h3>{m.name}</h3><p>{m.description||"No description"}</p></div><DeleteMeal id={m.id}/></div>)}</div>:<p className="muted small">No meal options yet. Add choices before sending invitations.</p>}</div></section></div><aside className="settings-side"><section className="card card-pad"><div className="settings-icon"><ShieldCheck/></div><h2>Administrators</h2><p className="muted small">{members?.length||0} people can access this wedding workspace.</p><div className="member-list">{members?.map((m,i)=><div key={m.id}><span className="avatar">{i+1}</span><div><strong>{`Administrator ${i+1}`}</strong><small>{m.role}</small></div></div>)}</div><div className="admin-note"><UserPlus/><p>Create the second user in Supabase Authentication, then add them to <code>wedding_users</code>. The exact command is in the README.</p></div></section><section className="card card-pad"><h3>Current schedule</h3><dl className="settings-summary"><div><dt>Wedding</dt><dd>{formatDate(wedding.wedding_date)}</dd></div><div><dt>RSVP deadline</dt><dd>{formatDate(wedding.rsvp_deadline)}</dd></div><div><dt>Public form</dt><dd><a href="/rsvp" target="_blank">/rsvp</a></dd></div></dl></section></aside></div></div>}
function Field({name,label,value,type="text",required=false}:{name:string;label:string;value:unknown;type?:string;required?:boolean}){return <div className="field"><label htmlFor={name}>{label}</label><input className="input" id={name} name={name} type={type} defaultValue={String(value??"")} required={required}/></div>}
