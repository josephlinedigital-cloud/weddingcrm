import { CalendarRange, LockKeyhole } from "lucide-react";
import { notFound } from "next/navigation";
const phases:Record<string,{title:string;phase:string;description:string}>={settings:{title:"Settings",phase:"Phase 1",description:"Wedding details and administrator management are configured from Supabase during the foundation phase."}};
export default async function PlannedPage({params}:{params:Promise<{section:string}>}){const {section}=await params;const item=phases[section];if(!item)notFound();return <div className="page"><div className="page-header"><div><p className="eyebrow">{item.phase}</p><h1>{item.title}</h1></div></div><div className="card planned-card"><div className="empty-icon"><CalendarRange/></div><h2>{item.title} is prepared for {item.phase}</h2><p>{item.description}</p><div className="planned-note"><LockKeyhole/>The normalised tables, relationships and private access policies for this module are already included in the database migration.</div></div></div>}
